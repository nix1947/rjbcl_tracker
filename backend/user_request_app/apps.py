from django.apps import AppConfig


class UserRequestAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'user_request_app'
    verbose_name = 'New User Requests'  # Change this to your desired name

